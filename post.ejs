<!DOCTYPE html>
<html lang="zh-CN">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title><%= post.title %>｜Firo 中文社区</title>
    <meta name="description" content="<%= post.properties.description %>">
    <meta property="og:image" content="<%= post.properties.img || 'https://firo.org/img/meta/apple-touch-icon.png' %>">
    <meta property="og:title" content="<%= post.title %>">
    <meta property="og:description" content="<%= post.properties.description %>">
    <meta name="twitter:title" content="<%= post.title %>">
    <meta name="theme-color" content="#9b1c2e">
    <link rel="stylesheet" href="css/style.css">
    <link rel="apple-touch-icon" sizes="180x180" href="https://firo.org/img/meta/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="https://firo.org/img/meta/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="https://firo.org/img/meta/favicon-16x16.png">
    <base target="_blank" />
  </head>
  <body>
    <nav>
      <a href="index.html" class="logo" target="_self">
        <img class="logo-img-default" src="<%= imgsURL %>/Firo logo.svg" alt="Firo 中文社区" height="84">
        <img class="logo-img-dark-mode" src="<%= imgsURL %>/Firo logo dark mode.svg" alt="Firo 中文社区" height="84">
      </a>
      <ul class="menu">
        <% if (!post.filename.startsWith('community.')) { %>
          <li><a href="community.html" target="_self">社区</a></li>
        <% } %>
        <li><a href="index.html" target="_self">动态</a></li>
      </ul>
    </nav>
    <main>
      <article>
        <% if (post.properties.title) { %>
          <header>
            <h1><%= post.properties.title %></h1>
            <% if (post.properties.summary) { %>
              <p><em><%= post.properties.summary %></em></p>
            <% } %>
            <p class="article-header-bar">
              <%= post.dateLocale %>
              <% if (post.properties.source) { %>
                ｜
                <a href="<%= post.properties.source %>">
                  <%= post.properties.type === 'video' ? '原视频' : '原文' %>
                </a>
              <% } %>
            </p>
          </header>
        <% } %>
        <%- post.html %>
        <% if (post.properties.iframe) { %>
          <noscript>需要浏览器启用 JavaScript</noscript>
          <p class="iframe-wrapper">
            <iframe onload="setTimeout(function () { document.querySelector('.iframe-wrapper').classList.add('loaded') }, 100)" src="<%= post.properties.iframe %>" importance="high" title="视频嵌入页面" frameborder="0" allowfullscreen scrolling="no" width="640" height="480"></iframe>
          </p>
        <% } %>
        <% if (post.properties.source && (!post.properties.type || post.properties.type === 'article')) { %>
          <footer><a href="<%= post.properties.source %>">原文</a></footer>
        <% } %>
      </article>
    </main>
  </body>
</html>
